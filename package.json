{
  "name": "zenn-stats-generator",
  "version": "1.0.0",
  "description": "Generate Zenn stats image using GitHub Actions",
  "main": "scripts/generate-zenn-stats.js",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "generate": "node scripts/generate-zenn-stats.js"
  },
  "dependencies": {
    "axios": "^1.7.0"
  }
}
